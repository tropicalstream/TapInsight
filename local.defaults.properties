# Committed defaults for secrets-gradle-plugin.
# Do NOT put real secrets here — this file is tracked in git.
# NOTE: GEMINI_API_KEY is intentionally NOT defined here. It is generated
# as a BuildConfig field in app/build.gradle.kts from an optional gradle
# property (the secrets plugin emits invalid Java for blank values).
# Real keys are configured at runtime in the companion app.
