# Committed defaults for secrets-gradle-plugin.
# Do NOT put real secrets here — this file is tracked in git.
# Real keys go in local.properties (gitignored) or the companion app
# settings at runtime. The value below must stay QUOTED — the plugin
# pastes it verbatim into BuildConfig.java.
GEMINI_API_KEY=""
