<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!--
      Phase 2 Step 2f.2: foreground service permissions for the
      GeminiSessionForegroundService. FOREGROUND_SERVICE is needed
      since Android 9 (API 28); FOREGROUND_SERVICE_MICROPHONE is the
      Android 14+ (API 34) granular flavour required to keep the
      AudioRecord open while visionclaw is backgrounded. POST_-
      NOTIFICATIONS is needed since Android 13 (API 33) for the FGS
      notification to actually show.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <!--
      Media access for the photos gallery's DCIM merge: lets the
      companion app + on-glasses gallery enumerate the RayNeo native
      Camera app's photos & videos (in /DCIM/Camera) alongside
      TapInsight's own /Android/data/com.rayneo.visionclaw/files/Media/
      Photos. READ_MEDIA_IMAGES / READ_MEDIA_VIDEO are the API 33+
      granular replacements for READ_EXTERNAL_STORAGE; the legacy
      permission stays declared with maxSdkVersion=32 so older
      Android builds still work.
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="true" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.VisionClawRayNeo"
        tools:targetApi="31">

        <meta-data
            android:name="com.rayneo.mercury.app"
            android:value="true" />

        <!-- Removed ar_mode to enable binocular (both lenses) display.
             ar_mode restricts rendering to left lens only. -->

        <!-- Phase 2 Step 2f: launcher intent filter moved to the
             tapbrowser module's MainActivity so the browser is the
             entry point (unipanel design). visionclaw stays
             exported=true so the new btnQuit-launches-chat path
             from tapbrowser can REORDER_TO_FRONT this Activity, and
             the reverse warm-start can spawn it in the background
             on cold launch. -->
        <activity
            android:name=".MainActivity"
            android:configChanges="density|orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|keyboard|navigation"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustNothing"
            android:theme="@style/Theme.VisionClawRayNeo" />

        <!--
          Phase 2 Step 2f.2: foreground service that keeps the
          Gemini Live AudioRecord alive when visionclaw is in the
          background. Type=microphone is mandatory on Android 14+
          for any FGS that wants ongoing mic capture. exported=false
          because the service is only started/stopped by visionclaw
          MainActivity from inside the app.
        -->
        <service
            android:name=".core.session.GeminiSessionForegroundService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

    </application>

</manifest>
